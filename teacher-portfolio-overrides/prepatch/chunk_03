Card(sec:Section,e:Evidence):View {
@@ -177,7 +226,7 @@
         listOf("العنوان" to title,"التاريخ" to date,"وصف مختصر" to desc,"الرابط" to link).forEach{(l,v)->box.addView(tv(l,12f,inkSoft,true));box.addView(v,LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,if(v===desc)dp(92) else dp(50)).apply{bottomMargin=dp(8)})}
         pendingEvidenceFileLabel=tv(existing?.fileName?.let{"المرفق الحالي: $it"}?:"لم يتم اختيار ملف",11f,muted)
         box.addView(btn("اختيار صورة أو PDF أو ملف",inkSoft,Color.WHITE){pickEvidenceFile()},LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,dp(46)));box.addView(pendingEvidenceFileLabel)
-        val d=AlertDialog.Builder(this).setTitle(if(existing==null)"إضافة شاهد" else "تعديل الشاهد").setView(ScrollView(this).apply{addView(box)}).setNegativeButton("إلغاء",null).setPositiveButton("حفظ",null).create()
+        val d=AlertDialog.Builder(this).setTitle(if(existing==null)"إضافة شاهد" else "تعديل الشاهد").setView(ScrollView(this).apply{addView(box)}).setNegativeButton("✕ إغلاق",null).setPositiveButton("حفظ",null).create()
         d.setOnShowListener{
             styleDialog(d); d.getButton(AlertDialog.BUTTON_POSITIVE).setOnClickListener{
                 val t=title.text.toString().trim();if(t.isBlank()){title.error="اكتب عنوان الشاهد";return@setOnClickListener}
@@ -193,23 +242,37 @@
 
     private fun showSectionManager() {
         currentSectionId="__settings__"
-        val scroll=ScrollView(this);val box=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;setPadding(dp(18),dp(18),dp(18),dp(36))}
-        val h=LinearLayout(this).apply{orientation=if(isTablet)LinearLayout.HORIZONTAL else LinearLayout.VERTICAL;gravity=Gravity.CENTER_VERTICAL};h.addView(sectionTitle("إدارة أقسام القائمة","تعديل أو إخفاء الأقسام مع الاحتفاظ بشواهدها، واستعادة الأقسام المخفية لاحقًا."),LinearLayout.LayoutParams(0,LinearLayout.LayoutParams.WRAP_CONTENT,1f));h.addView(btn("＋ قسم جديد",gold){showSectionDialog(null)},LinearLayout.LayoutParams(if(isTablet)dp(135) else LinearLayout.LayoutParams.MATCH_PARENT,dp(48)));box.addView(h)
+        val page=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;setBackgroundColor(paper)}
+        val fixed=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;setPadding(dp(16),dp(12),dp(16),dp(10));setBackgroundColor(paper)}
+        if(isTablet){
+            val titleRow=LinearLayout(this).apply{orientation=LinearLayout.HORIZONTAL;gravity=Gravity.CENTER_VERTICAL}
+            titleRow.addView(sectionTitle("إدارة أقسام القائمة","تعديل أو إخفاء الأقسام مع الاحتفاظ بشواهدها، واستعادة الأقسام المخفية لاحقًا."),LinearLayout.LayoutParams(0,LinearLayout.LayoutParams.WRAP_CONTENT,1f))
+            titleRow.addView(closeButton("إغلاق"){showProfile()},LinearLayout.LayoutParams(dp(120),dp(46)).apply{marginStart=dp(8)})
+            fixed.addView(titleRow)
+        } else {
+            fixed.addView(closeButton("إغلاق"){showProfile()},LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,dp(46)))
+            fixed.addView(sectionTitle("إدارة أقسام القائمة","تعديل أو إخفاء الأقسام مع الاحتفاظ بشواهدها، واستعادة الأقسام المخفية لاحقًا."),LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,LinearLayout.LayoutParams.WRAP_CONTENT).apply{topMargin=dp(10)})
+        }
+        fixed.addView(btn("＋ قسم جديد",gold){showSectionDialog(null)},LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,dp(48)))
+        page.addView(fixed)
+
+        val scroll=ScrollView(this);val box=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;setPadding(dp(16),dp(10),dp(16),dp(36))}
         box.addView(tv("الأقسام الظاهرة",17f,ink,true));db.getSections(true).filter{!it.hidden}.forEach{s->box.addView(sectionRow(s,false))}
         box.addView(tv("الأقسام المخفية",17f,ink,true).apply{setPadding(dp(4),dp(22),dp(4),dp(8))});val hidden=db.getSections(true).filter{it.hidden};if(hidden.isEmpty())box.addView(tv("لا توجد أقسام مخفية.",13f,muted)) else hidden.forEach{s->box.addView(sectionRow(s,true))}
-        scroll.addView(box);setScreen(scroll)
+        scroll.addView(box);page.addView(scroll,LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,0,1f));setScreen(page)
     }
     private fun sectionRow(s:Section,hidden:Boolean):View {
         val row=LinearLayout(this).apply{orientation=if(isTablet)LinearLayout.HORIZONTAL else LinearLayout.VERTICAL;gravity=Gravity.CENTER_VERTICAL;background=bg(Color.WHITE,11,line);setPadding(dp(12),dp(10),d