p(12),dp(10))}
-        val info=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;addView(tv(s.title,14f,ink,true));if(s.description.isNotBlank())addView(tv(s.description,11f,muted))};row.addView(info,LinearLayout.LayoutParams(0,LinearLayout.LayoutParams.WRAP_CONTENT,1f))
-        val acts=LinearLayout(this).apply{orientation=LinearLayout.HORIZONTAL}
+        val info=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;addView(tv(s.title,14f,ink,true));if(s.description.isNotBlank())addView(tv(s.description,11f,muted))}
+        row.addView(info,if(isTablet) LinearLayout.LayoutParams(0,LinearLayout.LayoutParams.WRAP_CONTENT,1f) else LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,LinearLayout.LayoutParams.WRAP_CONTENT).apply{bottomMargin=dp(8)})
+        val acts=LinearLayout(this).apply{orientation=LinearLayout.HORIZONTAL;gravity=Gravity.END}
         if(!hidden){acts.addView(btn("تعديل",Color.rgb(241,234,217),ink){showSectionDialog(s)},LinearLayout.LayoutParams(dp(82),dp(42)).apply{marginStart=dp(4)});acts.addView(btn("إخفاء",Color.rgb(241,234,217),ink){db.hideSection(s.id,true);showSectionManager()},LinearLayout.LayoutParams(dp(82),dp(42)))}
         else {acts.addView(btn("استعادة",Color.rgb(241,248,245),emerald){db.hideSection(s.id,false);showSectionManager()},LinearLayout.LayoutParams(dp(88),dp(42)).apply{marginStart=dp(4)});acts.addView(btn("حذف نهائي",Color.rgb(251,233,229),brick){confirm("حذف نهائي","سيتم حذف القسم وكل شواهده ومرفقاته نهائيًا."){db.getEvidence(s.id).forEach{deleteEvidenceFile(it)};db.deleteSection(s.id);showSectionManager()}},LinearLayout.LayoutParams(dp(105),dp(42)))}
-        row.addView(acts);return row.apply{layoutParams=LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,LinearLayout.LayoutParams.WRAP_CONTENT).apply{bottomMargin=dp(8)}}
+        row.addView(acts,if(isTablet) LinearLayout.LayoutParams(LinearLayout.LayoutParams.WRAP_CONTENT,LinearLayout.LayoutParams.WRAP_CONTENT) else LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,LinearLayout.LayoutParams.WRAP_CONTENT));return row.apply{layoutParams=LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,LinearLayout.LayoutParams.WRAP_CONTENT).apply{bottomMargin=dp(8)}}
     }
     private fun showSectionDialog(existing:Section?) {
         val title=edit("عنوان القسم").apply{setText(existing?.title?:"")};val desc=edit("وصف القسم",true).apply{setText(existing?.description?:"")};val box=LinearLayout(this).apply{orientation=LinearLayout.VERTICAL;setPadding(dp(12),0,dp(12),0);addView(tv("عنوان القسم",12f,inkSoft,true));addView(title,LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,dp(50)));addView(tv("وصف القسم",12f,inkSoft,true));addView(desc,LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT,dp(95)))}
-        val d=AlertDialog.Builder(this).setTitle(if(existing==null)"إضافة قسم مخصص" else "تعديل القسم").setView(box).setNegativeButton("إلغاء",null).setPositiveButton("حفظ",null).create();d.setOnShowListener{styleDialog(d);d.getButton(AlertDialog.BUTTON_POSITIVE).setOnClickListener{val t=title.text.toString().trim();if(t.isBlank()){title.error="اكتب عنوان القسم";return@setOnClickListener};val s=existing?.copy(title=t,description=desc.text.toString().trim())?:Section("custom_${UUID.randomUUID()}",t,desc.text.toString().trim(),false,false,db.nextSectionOrder());db.upsertSection(s);d.dismiss();toast("تم حفظ القسم ✓");showSectionManager()}};d.show()
+        val d=AlertDialog.Builder(this).setTitle(if(existing==null)"إضافة قسم مخصص" else "تعديل القسم").setView(box).setNegativeButton("✕ إغلاق",null).setPositiveButton("حفظ",null).create();d.setOnShowListener{styleDialog(d);d.getButton(AlertDialog.BUTTON_POSITIVE).setOnClickListener{val t=title.text.toString().trim();if(t.isBlank()){title.error="اكتب عنوان القسم";return@setOnClickListener};val s=existing?.copy(title=t,description=desc.text.toString().trim())?:Section("custom_${UUID.randomUUID()}",t,desc.text.toString().trim(),false,false,db.nextSectionOrder());db.upsertSection(s);d.dismiss();toast("تم حفظ القسم ✓");showSectionManager()}};d.show()
     }
 
     private fun pickProfilePhoto(){ startActivityForResult(Intent(Intent.ACTION_OPEN_DOCUMENT).apply{addCategory(Intent.CATEGORY_OPENABLE);type="image/*"},PICK_PROFILE) }
